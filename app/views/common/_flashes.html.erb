<%
 f = {:notice =>  "alert-success",
      :error => "alert-error",
      :alert => 'alert-message'
 }
%>
<% [:notice, :error, :alert].each do |level| %>
  <% unless flash[level].blank? %>
    <div class="<%= f[level] %> alert fade in" data-alert="alert">
      <a class="close" data-dismiss="alert" href="#">×</a>
      <%= content_tag :p, flash[level] %>
    </div>
  <% end %>
<% end %>
